restorecon_xattr - manage extended attribute entries added by or


will display the SHA256 digests added to extended attributes or delete the
attribute completely. These attributes are set by or to specified directories
when relabeling recursively.

is useful for managing the extended attribute entries particularly when users
forget what directories they ran or from.

and filesystems do not support the extended attribute and are automatically
excluded from searches.

By default will display the SHA256 digests with "Match" appended if they match
the default specfile set or the set used with the option. Non-matching SHA256
digests will be displayed with "No Match" appended.  This feature can be
disabled by the option.

delete all non-matching directory digest entries.  delete all directory digest
entries.  do not read to obtain a list of non-seclabel mounts to be excluded
from relabeling checks.
Setting is useful where there is a non-seclabel fs mounted with a seclabel fs
mounted on a directory below this.  Do not append "Match" or "No Match" to
displayed digests.  recursively descend directories.  display SHA256 digest
generated by specfile set (Note that this digest is not used to match the
directory digest entries, and is shown for reference only).
directory to exclude (repeat option for more than one directory).
an optional containing file context entries as described in If the option is
not specified, then the default file_contexts will be used.

the pathname of the directory tree to be searched.

List all paths that where assigned a checksum by "restorecon/setfiles -D"
# restorecon_xattr -r /
Remove all non-matching checksums
# restorecon_xattr -rd /



