zcmp, zdiff - compare compressed files [ cmp_options ] file1 [ file2 ]
[ diff_options ] file1 [ file2 ] The and commands are used to invoke the or
the program on files compressed via All options specified are passed directly
to or If only is specified, it is compared to the uncompressed contents of If
two files are specified, their contents (uncompressed if necessary) are fed to
or The input files are not modified.  The exit status from or is preserved.
cmp(1), diff(1), zmore(1), zgrep(1), znew(1), zforce(1), gzip(1), gzexe(1)
Messages from the or programs may refer to file names such as "-" instead of
to the file names specified.


