loadunimap - load the kernel unicode-to-font mapping table [options] [map] The
command is obsolete - its function is now built-in into However, for backwards
compatibility it is still available as a separate command.  The program loads
the specified map in the kernel unicode-to-font mapping table.	If no map is
given mapping table is assumed.	 The default extension (that can be omitted)
is On Linux 2.6.1 and later one can specify the console device using the
option.	 Usually one does not call directly - its function is also built into
-o, --output=FILE save the old map to the FILE.	 -C, --console=DEV the console
device to be used.  -V, --version print version number.	 -h, --help print this
usage message.	The default directory for unicode mappings.  The default
mapping file.  Andries Brouwer <aeb@cwi.nl>



