zgrep - search possibly compressed files for a regular expression [
grep_options ] The command invokes on compressed or gzipped files.  All
options specified are passed directly to If no file is specified, then the
standard input is decompressed if necessary and fed to grep.  Otherwise the
given files are uncompressed if necessary and fed to If the GREP environment
variable is set, uses it as the program to be invoked.	Exit status is 0 for a
match, 1 for no matches, and 2 if trouble.  The following options are not
supported: and Charles Levert (charles@comm.polymtl.ca)


