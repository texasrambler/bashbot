ip - show / manipulate routing, network devices, interfaces and tunnels







-V[ersion] | -h[uman-readable] | -s[tatistics] | -d[etails] | -r[esolve] |
-iec | -f[amily] { -4 | -6 | -B | -0 | -l[oops] { maximum-addr-flush-attempts
} | -o[neline] | -rc[vbuf] [size] | -t[imestamp] | -ts[hort] | -n[etns] name |
-N[umeric] | -a[ll] | -c[olor] | -br[ief] | -j[son] | -p[retty] }


Print the version of the utility and exit.

output statistics with human readable values followed by suffix.

Read commands from provided file or standard input and invoke them.  First
failure will cause termination of ip.

Don't terminate ip on errors in batch mode.  If there were any errors during
execution of the commands, the application return code will be non zero.

Output more information. If the option appears twice or more, the amount of
information increases.	As a rule, the information is statistics or some time
values.

Output more detailed information.

Specify maximum number of loops the 'ip address flush' logic will attempt
before giving up. The default is 10.  Zero (0) means loop until all addresses
are removed.

Specifies the protocol family to use. The protocol family identifier can be
one of or If this option is not present, the protocol family is guessed from
other arguments. If the rest of the command line does not give enough
information to guess the family, falls back to the default one, usually or is
a special family identifier meaning that no networking protocol is involved.

shortcut for

shortcut for

shortcut for

shortcut for

shortcut for

output each record on a single line, replacing line feeds with the character.
This is convenient when you want to count records with or to the output.

use the system's name resolver to print DNS names instead of host addresses.

switches to the specified network namespace Actually it just simplifies
executing of:


to


Print the number of protocol, scope, dsfield, etc directly instead of
converting it to human readable name.

executes specified command over all objects, it depends if command supports
this option.

Configure color output. If parameter is omitted or color output is enabled
regardless of stdout state. If parameter is stdout is checked to be a terminal
before enabling color output. If parameter is color output is disabled. If
specified multiple times, the last one takes precedence. This flag is ignored
if is also given.

Used color palette can be influenced by environment variable (see

display current time when using monitor option.

Like but use shorter format.

Set the netlink socket receive buffer size, defaults to 1MB.

print human readable rates in IEC units (e.g. 1Ki = 1024).

Print only basic information in a tabular format for better readability. This
option is currently only supported by

Output results in JavaScript Object Notation (JSON).

The default JSON format is compact and more efficient to parse but hard for
most users to read.  This flag adds indentation for readability.

Request the kernel to send the applied configuration back.



- protocol (IP or IPv6) address on a device.

- label configuration for protocol address selection.

- Foo-over-UDP receive port configuration.

- manage identifier locator addresses (ILA).

- manage IOAM namespaces and IOAM schemas.

- tunnel ethernet over IP (L2TPv3).

- network device.

- MACsec device configuration.

- multicast address.

- watch for netlink messages.

- manage MPTCP path manager.

- multicast routing cache entry.

- rule in multicast routing policy database.

- manage ARP or NDISC cache entries.

- network configuration monitoring.

- manage network namespaces.

- manage nexthop objects.

- manage the neighbor cache's operation.

- routing table entry.

- rule in routing policy database.

- manage IPv6 segment routing.

- manage and show interface statistics.

- manage TCP Metrics.

- manage tokenized interface identifiers.

- tunnel over IP.

- manage TUN/TAP devices.

- manage virtual routing and forwarding devices.

- manage IPSec policies.

The names of all objects may be written in full or abbreviated form, for
example can be abbreviated as or just


Specifies the action to perform on the object.	The set of possible actions
depends on the object type.  As a rule, it is possible to and (or ) objects,
but some objects do not allow all of these operations or have some additional
commands. The command is available for all objects. It prints out a list of
available commands and argument syntax conventions.

If no command is given, some default command is assumed.  Usually it is or, if
the objects of this class cannot be listed,

If set, it's value is used for detection whether background is dark or light
and use contrast colors for it.

COLORFGBG environment variable usually contains either two or three values
separated by semicolons; we want the last value in either case.	 If this value
is 0-6 or 8, chose colors suitable for dark background:

COLORFGBG=";0" ip -c a

Exit status is 0 if command was successful, and 1 if there is a syntax error.
If an error was reported by the kernel exit status is 2.

ip addr Shows addresses assigned to all network interfaces.  ip neigh Shows
the current neighbour table in kernel.	ip link set x up Bring up interface x.
ip link set x down Bring down interface x.  ip route Show table routes.

was written by Alexey N. Kuznetsov and added in Linux 2.2.
Report any bugs to the Network Developers mailing list where the development
and maintenance is primarily done.  You do not have to be subscribed to the
list to send a message there.

Original Manpage by Michail Litvak <mci@owl.openwall.com>


