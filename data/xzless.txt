xzless, lzless - view xz or lzma compressed (text) files
(DEPRECATED) is a filter that displays text from compressed files to a
terminal.  Files supported by are decompressed; other files are assumed to be
in uncompressed form already.  If no are given, reads from standard input.
uses to present its output.  Unlike its choice of pager cannot be altered by
setting an environment variable.  Commands are based on both and and allow
back and forth movement and searching.	See the manual for more information.
The command named is provided for backward compatibility with LZMA Utils.  It
is deprecated and will be removed in a future version.	A list of characters
special to the shell.  Set by unless it is already set in the environment.
Set to a command line to invoke the decompressor for preprocessing the input
files to


