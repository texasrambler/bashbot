xzdec, lzmadec - Small .xz and .lzma decompressors
is a liblzma-based decompression-only tool for (and only files.	 is intended
to work as a drop-in replacement for in the most common situations where a
script has been written to use (and possibly a few other commonly used
options) to decompress files.  is identical to except that supports files
instead of files.  To reduce the size of the executable, doesn't support
multithreading or localization, and doesn't read options from and environment
variables.  doesn't support displaying intermediate progress information:
sending to does nothing, but sending terminates the process instead of
displaying progress information.  Ignored for compatibility.  supports only
decompression.	Ignored for compatibility.  never creates or removes any
files.	Ignored for compatibility.  always writes the decompressed data to
standard output.  Specifying this once does nothing since never displays any
warnings or notices.  Specify this twice to suppress errors.  Ignored for
compatibility.	never uses the exit status 2.  Display a help message and exit
successfully.  Display the version number of and liblzma.  All was good.  An
error occurred.	 doesn't have any warning messages like has, thus the exit
status 2 is not used by Use instead of or for normal everyday use.  or are
meant only for situations where it is important to have a smaller decompressor
than the full-featured and are not really that small.  The size can be reduced
further by dropping features from liblzma at compile time, but that shouldn't
usually be done for executables distributed in typical non-embedded operating
system distributions.  If you need a truly small decompressor, consider using
XZ Embedded.  XZ Embedded: <https://tukaani.org/xz/embedded.html>


