mtr-packet - send and receive network probes is a tool for sending network
probes to measure network connectivity and performance.	 Many network probes
can be sent simultaneously by a single process instance of and additional
probes can be generated by an instance of which already has network probes in
flight.	 It is intended to be used by programs which invoke it with Unix pipes
attached to its standard input and output streams.  reads command requests
from each separated by a newline character, and responds with command replies
to also each separated by a newline character.	The syntactic structure of
requests and replies are the same.  The following format is used: [...] is a
unique integer value.  The same value will be used as the for the response.
This is necessary for associating replies with requests, as commands may be
completed in a different order than they are requested.	 The invoker of should
always use the value to determine which command request has completed.	is a
string identifying the command request type.  A common command is which will
transmit one network probe.  strings and strings always come in pairs.	It is
a syntactic error to provide an without a corresponding Valid strings depend
on the being used.  Send a network probe to a particular IP address.  Either
an or argument must be provided.  A valid command will reply with or The
following arguments may be used: The Internet Protocol version 4 address to
probe.	The Internet Protocol version 6 address to probe.  The protocol to use
for the network probe.	and may be used.  The default protocol is The
destination port to use for or probes.	The local Internet Protocol version 4
address to use when sending probes.  The local Internet Protocol version 6
address to use when sending probes.  For probes, the local port number from
which to send probes.  The number of seconds to wait for a response to the
probe before discarding the probe as lost, and generating a command reply.
The time-to-live value for the Internet Protocol packet header used in
constructing the probe.	 This value determines the number of network hops
through which the probe will travel before a response is generated by an
intermediate network host.  The size of the packet used to send the probe, in
bytes, including the Internet Protocol header and transport protocol header.
The packet payload is filled with bytes of the value specified.	 Valid pattern
values are in the range 0 through 255.	In the case of IPv4, the "type of
service" field in the IP header is set to this value.  In the case of IPv6,
the "traffic class" field is set.  The packet mark value to be used by mark-
based routing.	(Available only on Linux.)  Check for support for a particular
feature in this version of and in this particular operating environment.  will
reply with A argument is required.  The name of a feature requested.  Some
features which can be checked are and The feature can be checked to retrieve
the version of The destination host received the probe and replied.  Arguments
of are: The Internet Protocol version 4 address of the host which replied to
the probe.  The Internet Protocol version 6 address of the host which replied
to the probe.  The time which passed between the transmission of the probe and
its response.  The time is provided as a integral number of microseconds
elapsed.  No response to the probe request was received before the timeout
expired.  The time-to-live value of the transmitted probe expired before the
probe arrived at its intended destination.  Arguments of are: The Internet
Protocol version 4 address of the host at which the time-to-live value
expired.  The Internet Protocol version 6 address of the host at which the
time-to-live value expired.  The time which passed between the transmission of
the probe and its response.  The time is provided as a integral number of
microseconds elapsed.  A list of Multiprotocol Label Switching values returned
with the probe response.  If the argument is present, one or more MPLS labels
will be represented by a comma separated list of values.  The values are
provided in groups of four.  The first four values in the list correspond to
the first MPLS label, the next four values correspond to the second MPLS
label, and so on.  The values are provided in this order: There was no route
to the host used in a request.	A probe could not be sent because the network
is down.  A probe could not be sent because there are already too many
unresolved probes in flight.  The operating system denied permission to send
the probe with the specified options.  The command request contained arguments
which are invalid.  A reply to provided to indicating the availability of a
particular feature.  The argument provided is: In most cases, the value will
be either indicating the feature is supported, or indicating no support for
the feature.  In the case that is the requested the version of is provided as
the value.  A controlling program may start as a child process and issue the
following command on 42 send-probe ip-4 127.0.0.1 This will send a network
probe to the loopback interface.  When the probe completes, mtr-packet will
provide a response on such as the following: 42 reply ip-4 127.0.0.1 round-
trip-time 126 This indicates that the loopback address replied to the probe,
and the round-trip time of the probe was 126 microseconds.  In order to trace
the route to a remote host, multiple commands, each with a different value,
are used.  11 send-probe ip-4 8.8.8.8 ttl 1 12 send-probe ip-4 8.8.8.8 ttl 2
13 send-probe ip-4 8.8.8.8 ttl 3 ...  Each intermediate host would respond
with a message, and the destination host would respond with a 11 ttl-expired
ip-4 192.168.254.254 round-trip-time 1634 12 ttl-expired ip-4 184.19.243.240
round-trip-time 7609 13 ttl-expired ip-4 172.76.20.169 round-trip-time 8643 14
ttl-expired ip-4 74.40.1.101 round-trip-time 9755 15 ttl-expired ip-4
74.40.5.126 round-trip-time 10695 17 ttl-expired ip-4 108.170.245.97 round-
trip-time 14077 16 ttl-expired ip-4 74.40.26.131 round-trip-time 15253 18 ttl-
expired ip-4 209.85.245.101 round-trip-time 17080 19 reply ip-4 8.8.8.8 round-
trip-time 17039 Note that the replies in this example are printed out of
order.	(The reply to probe 17 arrives prior to the reply to probe 16.)	 This
is the reason that it is important to send commands with unique token values,
and to use those token values to match replies with their originating
commands.  A Python 3.x package for sending asynchronous network probes using
mtr-packet is available.  See For the latest version, see the mtr web page at
For patches, bug reports, or feature requests, please open an issue on GitHub
at: TCP/IP Illustrated (Stevens, ISBN 0201633469).


